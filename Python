class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        for pos,i in enumerate(nums):
            if i<=target:
                if i == target:
                    return  pos
            else: 
                return pos
        return len(nums)
